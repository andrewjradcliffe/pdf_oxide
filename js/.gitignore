# Node modules
node_modules/
package-lock.json
yarn.lock

# Native module
*.node
*.so
*.dylib
*.dll

# npm publish staging
dist/
# Note: js/npm/<triple>/ directories ARE tracked (they hold per-platform
# subpackage templates — see js/npm/README.md). The build-time `.node`
# files dropped into them by CI are excluded by the *.node rule above.

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Build artifacts
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# Testing
test_output/
coverage/
